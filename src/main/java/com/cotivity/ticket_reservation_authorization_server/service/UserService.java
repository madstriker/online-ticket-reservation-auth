package com.cotivity.ticket_reservation_authorization_server.service;

import com.cotivity.ticket_reservation_authorization_server.dto.UserDTO;

public interface UserService {

    UserDTO add(UserDTO userDTO);
}
