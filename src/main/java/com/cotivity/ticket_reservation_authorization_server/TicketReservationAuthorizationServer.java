package com.cotivity.ticket_reservation_authorization_server;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class TicketReservationAuthorizationServer {

    public static void main(String[] args) {
            SpringApplication.run(TicketReservationAuthorizationServer.class);
    }
}
